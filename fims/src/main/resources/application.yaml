spring:
    datasource:
        url: "jdbc:mysql://127.0.0.1:3306/fims"
        username: "root"
        password: "123456"
    devtools:
        restart:
            enabled: true  # 设置开启热部署
            additional-paths: src/main/java # 重启目录
            exclude: WEB-INF/**
